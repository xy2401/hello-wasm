# WASI

WASI 为 WebAssembly 模块定义标准化系统接口，使程序能够在授权范围内访问参数、环境变量、时钟、随机数、文件描述符和文件系统。

## 与浏览器 API 的关系

WASI 不是浏览器 DOM API。浏览器中的 WASI 程序需要 JavaScript 兼容层把系统调用映射到内存文件系统、终端或受控网络实现。

## 能力边界

WASI 使用显式能力授权。没有预先开放的目录、套接字或设备，模块不能自行突破沙箱访问本机资源。

